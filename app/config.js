const config = {};

config.db = {};
config.webhost = 'https://url-shortener-ls8.herokuapp.com/';

// your MongoDB host and database name
// config.db.host = 'localhost';
config.db.name = 'url_shortener';

module.exports = config;
